const myNumber = Math.round(Math.random()*100);
let result;
if(0<= myNumber && myNumber<=10){
    switch(myNumber){
        case 0 :
        result= "nolla";
        break;
        case 1 :
            result= "yksi";
            break;
            case 2 :
                result= "kaksi";
                break;
                case 3 :
                    result= "kolme";
                    break;

    }
}